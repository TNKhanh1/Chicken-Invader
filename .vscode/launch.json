{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Run Game", 
            "type": "cppdbg",
            "request": "launch",

            "program": "${workspaceFolder}/ChickenInvader.exe",

            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}", 
            "environment": [],
            "externalConsole": false, 
            "MIMode": "gdb",

            "miDebuggerPath": "gdb",
            
            "preLaunchTask": "Build Game (g++)"
        }
    ]
}
